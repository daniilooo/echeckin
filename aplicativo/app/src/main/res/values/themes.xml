<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ECheckin" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:colorPrimary">@color/actionBar</item> <!-- Cor primária -->
        <item name="android:colorPrimaryDark">@color/secAction</item> <!-- Cor primária escura -->
        <item name="android:colorAccent">@color/cinzaBrilhante</item>
        <item name="android:windowBackground">@color/azure</item>
    </style>

    <style name="Theme.ECheckin" parent="Base.Theme.ECheckin" />

    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="telasApp" parent="Theme.Material3.DayNight">
        <item name="android:colorPrimary">@color/actionBar</item> <!-- Cor primária -->
        <item name="android:colorPrimaryDark">@color/secAction</item> <!-- Cor primária escura -->
        <item name="android:colorAccent">@color/cinzaBrilhante</item>
        <item name="android:windowBackground">@color/azure</item>
    </style>

    <style name="TituloTela">
        <item name="android:textSize">24sp</item> <!-- Tamanho do texto -->
        <item name="android:textColor">@color/primaryTextColor</item> <!-- Cor do texto -->
        <item name="android:textStyle">bold</item> <!-- Estilo do texto (negrito) -->
    </style>

</resources>